const initialState = {
  user: {},
  isLoggedIn: false,
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    case '':
      break;
    default:
      return state;
  }
};

export default userReducer;
